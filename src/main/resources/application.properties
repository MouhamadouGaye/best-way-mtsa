spring.application.name=moneytransfer
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
jwt.secret=${JWT_SECRET:yK49l0ZpTShq0hVgOZy2YunZtwKxvB7cA7m5YF6y+3w=}

stripe.secret-key=${STRIPE_SECRET_KEY}

# CORS configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}

# Server
server.port=8080
server.address=0.0.0.0

# Email Configuration
# Disable email configuration
spring.mail.enabled=false
spring.profiles.active=mail

spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:mgayeeeeee@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:fvzsgikfspqvavlv} 

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.autoconfigure.exclude: org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration

 

# App Configuration
app.base-url=${APP_BASE_URL}
app.support-email=${APP_SUPPORT_EMAIL}
app.company-name=${APP_COMPANY_NAME}
app.admin-email=${APP_ADMIN_EMAIL}


# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false


# Server Configuration
server.port=8080

# Email Configuration



# mvn spring-boot:run -Dspring-boot.run.arguments="--debug"
